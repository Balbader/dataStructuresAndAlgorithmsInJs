const addScoreToArr = (score: string, arr: string[]) => arr.push(score);
const removePreviousScoreFromArr = (arr: string[]) => arr.pop();

let finalArray = new Array();
let array = ['5', '2', 'C', 'D', '+'];
